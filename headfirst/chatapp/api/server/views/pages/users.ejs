<% layout('base.layout') -%>
<div class="jumbotron">
    <h1><%= appName %></h1>
    <p><%= tagline %></p>
    <ul>
    	<% users.map(function(user){ %>
    		<li><%= `${user.uid} - ${user.email}` %></li>
    	<% }); %>
    </ul>
</div>